# MySQL.  Versions 4.1 and 5.0 are recommended.

development:
  adapter: mysql
  encoding: utf8
  database: db_name
  pool: 200
  username: root
  password: <password>
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
  adapter: mysql
  encoding: utf8
  database: gdevvdi-test
  pool: 200
  username: root
  password: root
  host: localhost

production:
  adapter: mysql
  encoding: utf8
  database: gdevvdi
  pool: 200
  username: root
  password: notToBeCheckedIn
  host: <host>
